<div data-role="popup" data-position-to="window" id="popupBasic" class="ui-content">

  <div>
    <% if has_order? && current_order.has_items? %>
        <p>Det finns <b><%= articles_text(current_order) %></b> i din korg</p>

        <p><b><%= number_to_currency current_order.total_without_delivery, format: '%n %u'%></b> (exklusive frakt)</p>

        <p>
          <%= link_to 'Till kassan', checkout_path, class: 'checkout', data: {role: 'button' }%>
        </p>
    <% else %>
        <p><b>Varukorgen är tom.</b></p>
    <% end %>
  </div>
</div>
<script>
    // Instantiate the popup on DOMReady, and enhance its contents

</script>